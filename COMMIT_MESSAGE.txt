feat(admin): integrate Voltron tournament calendar and daily Today posts

- add public Voltron calendar provider using snapshot-meta/content endpoints
- avoid SignalR/WebSocket dependency and browser-session credentials
- parse Full Calendar timeline rows from data-utc attributes
- parse mini tournament start/end schedule from calendar content
- normalize source timestamps as UTC
- add semantic snapshot hashing so countdown/render noise does not trigger updates
- retain last good snapshot when Voltron refresh fails
- reject suspiciously empty calendar parses before touching Discord
- poll Voltron on a configurable refresh interval
- use snapshot-meta to skip unnecessary full calendar downloads when available
- add CALENDAR_CHANNEL_ID rolling 30-day tournament calendar
- list tournament starts grouped by date with matched ending timestamps
- use Discord timestamp tags so members see event times in their local timezone
- split long calendars safely under Discord message limits
- edit canonical calendar messages in place instead of reposting
- recover canonical calendar messages from Discord history after bot/Render restarts
- advance the rolling 30-day window when the local calendar date changes
- add TODAY_CHANNEL_ID daily tournament summary
- include starts, continues, ends, and mini tournaments in daily summary
- update the existing Today post when Voltron changes during the day
- add configurable VOLTRON_TODAY_TIME and timezone-aware daily scheduling
- add /calendar and /today member commands
- add /calendar-refresh and /calendar-status leadership commands
- validate Read Message History for calendar/today channels
- add generic persisted bot-state values for canonical message IDs
- add Voltron Render environment variables and setup documentation
- add parser, formatting, settings, and state regression tests
- correct roster nickname documentation to preserve verification-first security model

Tests: 23 passing
