1. Copy the updated files into the OZY Admin repository.
2. In Render, make sure the neutral CALENDAR_* variables in RENDER_ENV.txt exist.
3. Commit ALL current working-tree changes. The currently deployed Git HEAD is older than the local working tree.
4. Push to the branch Render deploys.
5. Redeploy/restart OZY Admin.
6. In Render logs, confirm the synchronized slash-command list contains: event-create, calendar, today, time.
7. In Discord, type /ev and confirm /event-create appears.
8. Run /calendar-refresh and confirm #calendar uses a separate triple-backtick block for every day.
9. Run /today and confirm mini events span the current R+0 through, but not including, the next R+0.
10. Run /event-create and create a test scheduled event.
