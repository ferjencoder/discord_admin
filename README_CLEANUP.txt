Pre-commit corrections for the current OZY Admin feature branch.

Changes:
- Updates stale tests from TROOP_LEVEL_ROLE_MAP to LANGUAGE_ROLE_MAP/profile flow.
- Updates setup documentation to post-verification language + G/M/S.
- Corrects Render state_backend documentation to web-snapshot.
- Corrects README production state variables.

Validation:
- Full test suite: 52 passed.

These files do NOT perform the larger architecture refactor. Keep that as a
separate commit after the feature commit is stable.
